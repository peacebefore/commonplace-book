import React from 'react';
import { NavContainer, Nav } from './nav-bar.styles';

export const NavBar = () => {
  return (
    <NavContainer>
      <Nav> CommonPlace Book </Nav>
    </NavContainer>
  );
};
